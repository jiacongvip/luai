═══════════════════════════════════════════════════════════
  Nexus Agent Orchestrator - 宝塔一键安装
═══════════════════════════════════════════════════════════

🚀 方式一：直接运行（最简单）

在宝塔终端中执行：

bash <(curl -fsSL https://raw.githubusercontent.com/你的用户名/nexus-agent-orchestrator/main/install.sh)

或者：

curl -fsSL https://raw.githubusercontent.com/你的用户名/nexus-agent-orchestrator/main/install.sh | bash


📦 方式二：下载后运行

1. 在宝塔文件管理中上传 install.sh 到 /root/
2. 在宝塔终端执行：

cd /root
chmod +x install.sh
./install.sh


📋 安装过程

脚本会自动：
✅ 检测 Docker 环境
✅ 安装 Docker Compose（如果需要）
✅ 询问是否使用 Git 克隆项目
✅ 自动生成安全密码和密钥
✅ 询问域名和 API 密钥
✅ 自动构建和启动服务
✅ 自动运行数据库迁移


⚙️ 安装后配置

1. 在宝塔中创建网站
2. 设置反向代理：http://127.0.0.1:80
3. 配置 SSL 证书
4. 访问你的域名


📝 常用命令

查看日志：
docker-compose -f docker-compose.prod.yml logs -f

重启服务：
docker-compose -f docker-compose.prod.yml restart

停止服务：
docker-compose -f docker-compose.prod.yml stop

查看状态：
docker-compose -f docker-compose.prod.yml ps


═══════════════════════════════════════════════════════════

